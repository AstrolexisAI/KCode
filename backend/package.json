{
  "name": "kcode-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "db:migrate": "bun run src/db.ts"
  },
  "dependencies": {
    "hono": "^4.7.0"
  }
}
